<% display_columns_class(record.class).each do |config| %>
  <% column, css_class = config.to_a.flatten %>
  <div class='col <%= css_class %>'>
    <p>
      <strong><%= record.class.human_attribute_name(column) %>: </strong>
      <%= display_value(record, column) %>
    </p>
  </div>
<% end %>
