<div class='py-4'>
  <div class='row row-cols-1 g-4'>
    <% instance_variable_get("@#{controller_name}").each do |record| %>
      <div class='col'>
        <%= render "#{controller_name}/#{model_name}", model_name.to_sym => record %>
      </div>
    <% end %>
  </div>
</div>
