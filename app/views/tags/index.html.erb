<%= render 'shared/result_messages', record: @tag %>
<%= render 'shared/index_header' %>

<div id='tags'>
  <% @tags.each do |tag| %>
    <%= render tag %>
  <% end %>
</div>

<%= link_to 'New tag', new_tag_path %>
