<h1>Tag</h1>

<%= render 'shared/result_messages', record: @tag %>

<%= render @tag %>

<div>
  <%= link_to 'Edit this tag', edit_tag_path(@tag) %> |
  <%= link_to 'Back to tags', tags_path %>

  <%= button_to 'Destroy this tag', @tag, method: :delete, data: { turbo_confirm: 'Are you sure ?' } %>
</div>
