<%= render 'shared/result_messages', record: @review %>

<%= render @review %>

<div>
  <%= link_to 'Edit this review', edit_review_path(@review) %> |
  <%= link_to 'Back to reviews', reviews_path %>

  <%= button_to 'Destroy this review', @review, method: :delete, data: { turbo_confirm: 'Are you sure ?' } %>
</div>
