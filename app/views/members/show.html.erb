<%= render 'shared/result_messages', record: @member %>

<%= render @member %>

<div>
  <%= link_to 'Edit this member', edit_member_path(@member) %> |
  <%= link_to 'Back to members', members_path %>

  <%= button_to 'Destroy this member', @member, method: :delete, data: { turbo_confirm: 'Are you sure ?' } %>
</div>
