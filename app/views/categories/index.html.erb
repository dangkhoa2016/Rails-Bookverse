<%= render 'shared/result_messages', record: @category %>
<%= render 'shared/index_header' %>

<div id='categories'>
  <% @categories.each do |category| %>
    <%= render category %>
  <% end %>
</div>

<%= link_to 'New category', new_category_path %>
