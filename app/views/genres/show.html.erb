<%= render 'shared/result_messages', record: @genre %>

<%= render @genre %>

<div>
  <%= link_to 'Edit this genre', edit_genre_path(@genre) %> |
  <%= link_to 'Back to genres', genres_path %>

  <%= button_to 'Destroy this genre', @genre, method: :delete, data: { turbo_confirm: 'Are you sure ?' } %>
</div>
