<div id='<%= dom_id genre %>' class='card mt-3'>
  <div class='card-header border-success border-top border-bottom-0 border-4'>
    <h4 class='mb-0'><%= genre.name %></h4>
  </div>

  <div class='card-body row row-cols-1'>
    <%= render 'shared/display_fields', record: genre %>
  </div>

  <%= render 'shared/footer_actions', record: genre %>
</div>
