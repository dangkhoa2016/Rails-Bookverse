<%= render 'shared/result_messages', record: @publisher %>

<%= render @publisher %>

<div>
  <%= link_to 'Edit this publisher', edit_publisher_path(@publisher) %> |
  <%= link_to 'Back to publishers', publishers_path %>

  <%= button_to 'Destroy this publisher', @publisher, method: :delete, data: { turbo_confirm: 'Are you sure ?' } %>
</div>
